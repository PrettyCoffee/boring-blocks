import { Markdown } from "@storybook/addon-docs/blocks";
import { css, styled } from "storybook/theming";

import { useDarkMode } from "../.storybook/addons/dark-mode";
import { lightTheme, darkTheme } from "../.storybook/themes";
import Readme from "../../README.md?raw";
import { version } from "../../package.json"

export const Logo = styled.div(({ src }) => css`
  max-width: 16rem!important;
  aspect-ratio: 1 / 1;
  width: 100%;
  margin: 0 auto 3rem auto;
  position: relative;

  ::before, ::after {
    content: "";
    display: block;
    background: url(${src});
    position: absolute;
    inset: 0;
  }

  ::before {
    scale: 1.2;
    filter: blur(1.5rem);
    opacity: 0.5;
  }

  ::after {
    z-index: 1;
  }
`);

export const Version = styled.span`
  position: relative;
  ::after {
    content: "v${version}";
    position: absolute;
    bottom: 0.5rem;
    font-size: 1rem;
    white-space: nowrap;
  }
`

<Logo src="./sloth.png" alt="" />

<Version/>

<Markdown>{Readme.replace("boring-blocks", `boring-blocks`)}</Markdown>
